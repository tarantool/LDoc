--- Extra module with some extra funcs

--- Local funcs that would not create doc.
-- It is a specialized splitting operation on a string
-- @param text string
-- @return a table of substrings
local function local_foo(text)
end

--- foo explodes text.
-- It is a specialized splitting operation on a string.
-- @param text the string
-- @return a table of substrings
function foo (text)
end